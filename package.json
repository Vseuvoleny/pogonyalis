{
  "name": "pogonyalis-monorepo",
  "private": true,
  "packageManager": "pnpm@8.10.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "turbo": "^1.11.1",
    "typescript": "^5.5.4"
  }
}
